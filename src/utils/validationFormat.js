class ValidationFormatClass {
    // // Method to validate if the duration is in "hh:mm:ss" format
    // isValidDuration(duration) {
    //   const regex = /^([01]\d|2[0-3]):([0-5]\d):([0-5]\d)$/;
    //   return regex.test(duration);
    // }

  }

// const validationFormatClass = new ValidationFormatClass();

// module.exports = validationFormatClass; 
